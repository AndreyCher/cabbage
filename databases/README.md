# Databases

Database-owned assets live here. PostgreSQL schema changes are managed by the
Controller Alembic environment in `postgres/migrations`. Redis is operational
storage and has no persistent schema; its key/stream contract is documented by
the Controller.
